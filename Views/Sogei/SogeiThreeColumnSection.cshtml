@using Sitecore.XA.Foundation.MarkupDecorator.Extensions
@using Sitecore.XA.Foundation.RenderingVariants.Extensions
@using Sitecore.XA.Foundation.RenderingVariants.Fields
@using Sitecore.XA.Foundation.SitecoreExtensions.Extensions
@using Sitecore.XA.Foundation.Variants.Abstractions.Fields
@using Sitecore.Mvc.Helpers
@using Sitecore.Mvc


@model Sitecore.XA.Foundation.Variants.Abstractions.Models.VariantsRenderingModel

@if (Model.DataSourceItem != null || Html.Sxa().IsEdit)
{
<section class="section section-primary">
    <div class="container-fluid py-3 py-md-4">
        <h2 class="text-white">Elenco delle CARD</h2>
        <div class="row">
            <div class="col-12 order-md-2">
                <div class="row">
                    <div class="col-12 col-md-6 col-xl-4 d-flex">
                        @Html.Sitecore().Placeholder("left-column")
                    </div>
                    <div class="col-12 col-md-6 col-xl-4 d-flex">
                        @Html.Sitecore().Placeholder("middle-column")
                    </div>
                    <div class="col-12 col-md-6 col-xl-4 d-flex">
                        @Html.Sitecore().Placeholder("rigth-column")
                    </div>
                </div>
            </div>
            <div class="col-12 order-md-1 text-center text-md-right mt-md-n5">
                <a href="" class="btn btn-default mt-3 mt-md-n1">Tutta la sezione</a>
            </div>
        </div>
    </div>
</section>
    @*<div class="component-content">
            @if (Model.DataSourceItem == null)
            {
            @Model.MessageIsEmpty
            }
            else
            {
            foreach (BaseVariantField variantField in Model.VariantFields)
            {
            @Html.RenderingVariants().RenderVariant(variantField, Model.Item, Model.RenderingWebEditingParams, Model)
            }
            }
        </div>*@
}